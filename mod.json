{
	"geode": "4.10.1",
	"gd": {
		"win": "2.2074",
		"android": "2.2074",
		"mac": "2.2074",
		"ios": "2.2074"
	},
	"id": "geming400.screwyou3",
	"name": "Screw You 3",
	"version": "v6.9.0",
	"developer": "Geming400",
	"description": "Screw you 2, but better",
	"early-load": true,
	"tags": [
		"bugfix",
		"content",
		"customization",
		"enhancement",
		"gameplay",
		"performance",
		"utility"
	],
	"settings": {
		"behavior-title": {
			"type": "title",
			"name": "Mod's behavior"
		},
		"enabled": {
			"name": "Enabled",
			"description": "If the mod is enabled or not",
			"type": "bool",
			"default": true
		},
		"intensity": {
			"name": "Intensity",
			"description": "How many classes will be 'killed' on death",
			"type": "int",
			"default": 1,
			"enable-if": "enabled",
			"control": {
				"slider": true,
				"input": true
			},
			"min": 1,
			"max": 50
		},
		"can-be-nullptr": {
			"name": "Can be nullptr",
			"description": "If the return value is allowed to be a value with no value",
			"type": "bool",
			"default": true,
			"enable-if": "enabled"
		},
		"nullptr-chance": {
			"name": "Nullptr chance",
			"description": "The chance for the value to be nullptr",
			"type": "int",
			"default": 50,
			"enable-if": "enabled",
			"min": 0,
			"max": 100
		},

		"gibberish-data-title": {
			"type": "title",
			"name": "Mod's behavior (gibberish data)"
		},
		"gibberish-data-chance": {
			"name": "Gibberish data chance",
			"description": "The chance for gibberish data to be sent instead. If this doesn't execute the base function will not get executed",
			"type": "int",
			"default": 50,
			"enable-if": "enabled",
			"min": 0,
			"max": 100
		},
		"gibberish-data-numerical-min": {
			"name": "Gibberish data min (numerical)",
			"description": "The minimum value when a numerical value is encountered to send",
			"type": "float",
			"default": -100.0
		},
		"gibberish-data-numerical-max": {
			"name": "Gibberish data max (numerical)",
			"description": "The maximum value when a numerical value is encountered to send",
			"type": "float",
			"default": 100.0
		},
		"gibberish-data-string-lenght": {
			"name": "Gibberish data lenght (string)",
			"description": "The lenght of the string (filled with random characters) to send",
			"type": "int",
			"default": 5
		},

		"debug": {
			"name": "Debug",
			"type": "title"
		},
		"dump-classes": {
			"name": "Dump surviving classes",
			"description": "Dumps all of the surviving classes in geode's log",
			"type": "custom:dump-classes"
		},
		"reset-killed-func": {
			"name": "Reset killed functions",
			"description": "Resets the mod",
			"type": "custom:reset-killed-funcs"
		},
		"log": {
			"name": "Log debug stuff",
			"description": "If debug logs should be enabled. This will flood your console and possibly make your game slower",
			"type": "bool",
			"default": false
		}
	}
}
